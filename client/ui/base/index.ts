import { BREAK_POINT } from './break-point';
import { COLOR } from './color';
import { SIZE } from './size';

export const THEME = {
  color: COLOR,
  breakPoint: BREAK_POINT,
  size: SIZE
} as const;
