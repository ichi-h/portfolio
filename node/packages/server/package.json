{
  "name": "portfolio-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "spago build --watch --then 'spago run'",
    "build": "spago build"
  },
  "dependencies": {
    "purescript": "^0.15.9",
    "purty": "^7.0.0",
    "spago": "^0.21.0"
  }
}
